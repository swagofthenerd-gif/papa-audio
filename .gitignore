node_modules/
dist/
rpm-build/BUILD*/
rpm-build/RPMS/
rpm-build/SRPMS/
bridge-server/node_modules/
*.backup
.superpowers/
.qa/

# mpv's own screenshot path, if one ever escapes the configured directory
mpv-shot*.jpg
mpv-shot*.png

# Python bytecode from tools/
__pycache__/
*.pyc

# Local scaffolding: this worktree symlinks node_modules from the main
# checkout so it can run the suite without a second install. The entries
# above end in "/" and match a directory, not a symlink.
/node_modules
/bridge-server/node_modules
